import React from "react";
import { css } from "emotion";

import MenuPanel from "./MenuPanel";
import Logo from "./Logo";

export default () => {
  return (
    <div>
      <MenuPanel />
    </div>
  );
};
